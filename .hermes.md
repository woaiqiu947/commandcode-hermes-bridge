# CommandCode Hermes Bridge — 操作包协作说明

本文件是给 **Hermes agent** 看的项目指令。任何一台机器（mac / PC）的 Hermes 在此仓库目录内工作时，都会自动读到本文件。

## 这是什么

CommandCode（闭源订阅制 LLM 服务）↔ Hermes Agent 的**跨平台操作包**：手册 + 可复用脚本/配置/补丁，由 mac 与 PC 两台机器的 Hermes 共同维护。目标：**让任何一台机器都能快速用上同一个 CommandCode 订阅，且不用重新推理怎么装、怎么打补丁。**

## 仓库结构速览

```
├── docs/                  # 手册：SETUP（怎么装）+ WHY（为什么需要 bridge）
├── config/env.example     # 上游 .env 模板（平台无关）
├── patches/               # 本地补丁（平台无关，git apply 即可）
├── scripts/               # 验证/探测脚本（平台无关）
└── platforms/
    ├── macos/             # ← mac 的 Hermes 维护（已完成）
    └── pc/                # ← PC 的 Hermes 维护（待 PC 补充）
```

## 当前状态（2026-09-08）

- **mac 侧已完成**：`platforms/macos/` 有完整资产（launchd plist、脱敏 env 模板、README）。
- **平台无关资产已就位**：`patches/0001-canonical-models-only.patch`、`scripts/probe_bridge_models.py`、`config/env.example`。
- **PC 侧待办**：见下方"如果你是 PC 上的 Hermes"。

## 如果你是 PC 上的 Hermes（新机器，第一次见到本仓库）

你的任务：**整理这台 PC 的 CommandCode bridge 资产，补进 `platforms/pc/`，让仓库成为完整的多机操作包。**

### 步骤

1. **先读** `platforms/pc/README.md` —— 里面有本目录待补充内容的完整清单（操作系统→自启方式、env 模板、路径约定、与 mac 的差异）。
2. **判断操作系统**：
   - Windows → 自启用 nssm / 任务计划程序 / 启动文件夹；参考 bridge 源码 `release/` 是否有 Windows 方案。
   - Linux → 自启用 systemd unit（上游 `release/systemd/` 自带，可直接引用）。
3. **整理资产**（对照 mac 侧 `platforms/macos/` 的结构，做 PC 版）：
   - `platforms/pc/README.md`：填上 PC 的路径约定、与 mac 的差异、维护记录。
   - `platforms/pc/env.<系统>.example`：PC 实际 `.env` 的脱敏模板（key 一律 `<填入你的值>`，**严禁提交真实 key**）。
   - PC 的自启配置文件（`.service` / `.cmd` / 任务计划 XML 等）。
4. **判断是否需要补丁**：检查 PC 上 bridge 版本与 `patches/` 是否匹配——若 `src/config.ts` 还没有那两处本地定制，用 `git apply patches/0001-canonical-models-only.patch` 重打（打完 `npm run build`）。补丁内容详解见 `docs/SETUP-NEW-MACHINE.md` 第 8 节。
5. **平台无关资产直接复用**：`config/env.example`、`scripts/probe_bridge_models.py`、`docs/` 里的手册——不用重新整理，直接用。
6. **提交并推送**：commit 你的 PC 资产（commit message 说明加了什么），push 到 origin。**注意**：push 前确认 GitHub 凭据可用（`gh auth status`），不可用就提示用户配。

### 完成后检查

- [ ] `platforms/pc/README.md` 填满（不再是占位）
- [ ] 至少有一个 PC 专属资产文件（env 模板 / 自启配置）
- [ ] 无任何真实 key / token 提交（自查：`grep -rnE '(sk-|ghp_|[a-f0-9]{40})' platforms/`）
- [ ] commit + push 完成

## 如果你是 mac 上的 Hermes

mac 侧资产已完成（`platforms/macos/`）。你的职责：

- 不要重复整理 mac 资产。
- 若 PC 侧有新 commit（`platforms/pc/` 更新），可帮忙 review 是否有 key 泄漏、结构是否对齐。
- 维护记录写进对应平台的 README，不改本文件的"当前状态"日期除非结构有大变化。

## 安全铁律（所有机器）

- **严禁**把 `COMMANDCODE_API_KEY`、`BRIDGE_API_KEY`、`COMMANDCODE_API_KEYS`、任何 token 提交进仓库。
- env 模板一律脱敏：真实值换 `<填入你的值>`。
- 提交前自查：`grep -rnE '(sk-[A-Za-z0-9]{10,}|ghp_[A-Za-z0-9]{20,}|[a-f0-9]{40})' --include='*' . | grep -v '^./.git/'`
- bridge 只监听 `127.0.0.1`，不要把端口暴露到公网。
